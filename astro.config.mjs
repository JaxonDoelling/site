import { defineConfig } from 'astro/config';

export default defineConfig({
  // Used for RSS feed absolute URLs and canonical links.
  // Update to the real domain once one is bought, or to the Vercel
  // subdomain (e.g. https://your-project.vercel.app) once deployed.
  site: 'https://site-blond-theta-21.vercel.app',
});
